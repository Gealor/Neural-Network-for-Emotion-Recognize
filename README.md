# Цель проекта
Написать собственную нейронную сеть для задач распознавания звуков.
В данной ветке я проверяю новую архитектуре CRNN.

# Описание
В проекте представлено три основных python скрипта:
- download_dataset: для скачивания датасета RAVDRESS(https://www.kaggle.com/datasets/uwrfkaggler/ravdess-emotional-speech-audio?resource=download)
- prepare_data_v2: для подготовки данных из нескольких датасетов для обучения модели(с аугментацией)
- model_training: хранится непосредственная архитектура модели, а так же визуализация результатов обучения (старая версия)
- model_training_v2_test: новая архитектура модели, сфокусированная на новой версии prepare_data_v2


# Предварительные действия:
1. Создайте виртуальное окружение командой
```
python -m env .venv
```
2. Установите зависимости из файла requirements.txt
3. Выполните скрипт download_dataset.py и перенесите датасеты из указанных папок в dataset и соответствующую папку
4. Запустить подготовку данных скриптом prepare_data_v2.py
5. После этого можно запускать model_training_v2_test.py

# ЗАМЕЧАНИЯ
В нативной Windows системе отсутствует поддержка обучения на CUDA ядрах, для обучения на видеокарте я использую WSL2. Гайд о том как настроить WSL2 для машинного обучения на видеокарте: https://www.youtube.com/watch?v=402DciWGvt8


# Рекомендации и попытки повысить точность модели (13.02.2026 - 24.02.2026):
1. В DataGenerator вместо глобальных mean и std использую усреднение для каждой записи с помощью z-score
2. После RNN слоев, перед классификатором поставил Context-Aware Attention блок
3. Заменил функцию активации с relu на elu (вместо max(0, x) используется экспонента e^x)
4. Попробовать SpatialDropout2D вместо Dropout
5. Исправил извлечение признаков из аудиозаписи, добавил обрезание тишины с помощью trim, а так же в padding заполнение до нужной длины заполняется минимальным значением децибел(-80 для задач распознавания звуков) в записи
6. Попробовать из аудио записи извлекать первые и вторые производные, чтобы выделить границы начала звука. + вместо того, чтобы добавлять тишину справа от записи, нужно центрировать запись, т.е. заполнять слева и справа до нужной длины.
7. К пункту 6 добавить SE-блоки, чтобы определять, какой канал анализировать.

# Результаты
Удалось добиться стабильных 55-57% точности, но проблема с эмоцией happy до сих пор осталась. С результатами можно ознакомиться в папке trying_increase_accuracy_model(24_02_2026)


